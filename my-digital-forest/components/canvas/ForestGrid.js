"use client";

import { useFrame } from "@react-three/fiber";

export default function ForestGrid({ analyser }) {
  // Por ahora, solo devolvemos un grupo vacío para descartar errores
  return <group />;
}